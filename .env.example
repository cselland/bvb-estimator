# SQLite (default). For serverless production, use Postgres: postgresql://...
DATABASE_URL="file:./prisma/dev.db"

# Optional: send summary email via Resend (https://resend.com)
# RESEND_API_KEY=
# RESEND_FROM="Build vs Buy <reports@yourdomain.com>"
